# --- core ---
Django==6.0.7
djangorestframework==3.17.1
dj-database-url==3.1.2
python-dotenv==1.2.2
whitenoise==6.12.0
httpx>=0.27

# --- documents: page-aware extraction, figure detection, DOCX ---
pymupdf>=1.24
python-docx>=1.1
Pillow>=10.0

# --- faster vector maths on SQLite (optional but recommended) ---
numpy>=1.26

# --- async (optional: only if CELERY_ENABLED=True) ---
celery==5.6.3
redis==8.0.1

# --- other providers (optional) ---
# openai>=1.40
# anthropic>=0.40

# --- Postgres + pgvector (optional: only if DATABASE_URL is Postgres) ---
# psycopg[binary]>=3.2
# pgvector>=0.3

# --- production server ---
gunicorn>=23.0
